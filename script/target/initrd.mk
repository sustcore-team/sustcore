# The initrd-specific targets and recipes are generated by initrd.py during
# configure. Keep the host tool override here so the generated fragment does
# not duplicate global shell/toolchain defaults.
initrd-cpio ?= cpio
